---
apiVersion: v1
kind: Pod
metadata:
  name: pu-pr3
  labels: 
    app: pu-pr3
spec:
  containers:
     - image: ubuntu-ssh:v1.0
       name: pu-pr3
       ports:
         - containerPort: 22
  restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: pu-pr3
  namespace: default
  labels:
    app: pu-pr3
spec:
  type: ClusterIP
  selector:
    app: pu-pr3
  ports:
  - name: port-22-tcp
    protocol: TCP
    targetPort: 22
    port: 9027
  externalIPs:
  - "{{ controller_ip }}"
